This project uses Google Apps Script.

Backend:
- Written in .gs files
- Uses SpreadsheetApp as database

Frontend:
- HTML + vanilla JS
- Communicates via google.script.run

Important:
- Do NOT convert to Node.js unless explicitly requested
- Keep GAS style
